import { StyleSheet } from 'react-native'

export default (theme) => StyleSheet.create({
    normalBorder:{
        // todo
    },
    whiteBorder:{
        borderWidth: 2,
        borderColor: '#f1f1f1',
    },
})
